1. Abstract Factory Pattern is a creational design pattern, 
which solves the problem of creating entire product families without specifying their concrete classes.
2. Abstract Factory defines an interface for creating all distinct products 
 but leaves the actual product creation to concrete factory classes.
3. Abstract Factory patterns work around a super-factory which creates other factories.